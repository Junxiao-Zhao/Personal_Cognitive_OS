# Reference only: execution follows src/pco/checkpoint step modules.
# This YAML documents the PRD 18.3 step list and is not loaded by any runner.
version: consolidate@0.4.0
steps:
  - freeze_transcript
  - spawn_worker
  - validate_proposal
  - request_meta_approval
  - resume_after_rejection
  - validate_final
  - commit
  - render_context
  - publish_context
  - compact_parent
  - insert_receipt
  - close_worker
